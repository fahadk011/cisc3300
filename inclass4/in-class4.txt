1. In certain programming languages the source code is not directly translateed by the target machine. Instead, a different program, aka the interpreter, reads and executes the code, this is called an Interpreted language. 
2. Type checking is performed at runtime, means that the type of a variable is allowed to change over its lifetime, this is called Dynamic typing. 
3. JS for web browser work doesn’t tend to use classes to make objects, and is written in a more Prototype-based style. 
4. Variables that are block scoped, not declarable use the keyword let or const
5. Scope determines the accessibility (visibility) of variables. 
6. Variables assigned with the const keyword are block scoped and not redeclarable. 
7. Functions that can be passed as an argument to another function and called later are called First-class functions. 
8. Non primitive data types are (almost)all technically objects in memory. 
